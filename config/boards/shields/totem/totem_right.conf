# Right half (peripheral) configuration
# Only connects to left half

# Disable central role (peripheral only)
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n

# Allow only 1 Bluetooth connection (to left half only)
CONFIG_BT_MAX_CONN=1
CONFIG_BT_MAX_PAIRED=1
